{
  "name": "migration-lock-rehearsal-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:site",
    "build:site": "vite build --outDir dist/site",
    "test": "cargo test && node --test tests/*.test.mjs",
    "preview": "vite preview --outDir dist/site"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "playwright": "^1.58.2",
    "vite": "^6.1.0"
  }
}
