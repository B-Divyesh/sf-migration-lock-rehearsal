[
  {
    "id": "demo-report",
    "claim": "The bundled demo writes a go/no-go runbook.",
    "where": "landing demo terminal, README",
    "test": "npm test -- --test-name-pattern @claim:demo-report",
    "sandbox": "fresh temporary directory; mlr demo --dry-run; assert report.json and runbook.md"
  },
  {
    "id": "local-only",
    "claim": "It refuses remote database URLs.",
    "where": "landing privacy section, README",
    "test": "npm test -- --test-name-pattern @claim:local-only",
    "sandbox": "run mlr guard with a production-looking URL and assert a non-zero exit"
  },
  {
    "id": "site-no-third-party",
    "claim": "The documentation site loads no third-party scripts or fonts.",
    "where": "landing facts, README",
    "test": "npm test -- --test-name-pattern @claim:site-no-third-party",
    "sandbox": "build the static site and inspect emitted HTML, CSS, and JavaScript for http URLs"
  },
  {
    "id": "supported-engines",
    "claim": "Rehearses Postgres and ClickHouse migrations.",
    "where": "README",
    "test": "npm test -- --test-name-pattern @claim:supported-engines",
    "sandbox": "fresh temporary directory; run dry-run cards for Postgres and ClickHouse, then reject MySQL"
  },
  {
    "id": "demo-reset",
    "claim": "The CLI demo reset removes its named demo folder.",
    "where": ".factory/demo.md",
    "test": "npm test -- --test-name-pattern @claim:demo-reset",
    "sandbox": "fresh temporary directory; create a dry-run card, reset the explicit output directory, and assert it is gone"
  },
  {
    "id": "invented-sample",
    "claim": "The bundled demo uses invented customer records.",
    "where": "landing facts, README, demo page",
    "test": "npm test -- --test-name-pattern @claim:invented-sample",
    "sandbox": "read the shipped fixture and assert the documented invented records and no connection URL"
  },
  {
    "id": "chosen-output",
    "claim": "The CLI writes reports to the output folder you choose.",
    "where": "landing facts, privacy page, README",
    "test": "npm test -- --test-name-pattern @claim:chosen-output",
    "sandbox": "fresh temporary directory; run dry-run with an explicit output directory and assert both report files occur there"
  }
]
